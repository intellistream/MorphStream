package sesame.optimization.impl;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
public class TStorm extends PlanScheduler {
    private final static Logger LOG = LoggerFactory.getLogger(TStorm.class);
    @Override
    public SchedulingPlan Search(boolean worst_plan, int timeoutMs) {
        return null;
    }
}
